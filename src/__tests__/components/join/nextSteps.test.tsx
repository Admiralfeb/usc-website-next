import { render } from '@testing-library/react';
import { JoinNextSteps } from 'components/join';

describe('NextSteps', () => {
  it('should render', () => {
    render(<JoinNextSteps />);
  });
});
